{
  "name": "persian-personnel-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp index.html public/index.html && esbuild index.tsx --bundle --outfile=public/index.js --format=esm --jsx=automatic --external:react --external:\"react-dom/*\" --external:\"react/*\""
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "@vercel/postgres": "^0.10.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/xlsx": "^0.0.36",
    "@vercel/node": "^3.2.3",
    "typescript": "^5.5.3",
    "esbuild": "^0.21.4"
  }
}
